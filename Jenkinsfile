pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                sh 'echo "Hello world"'
                sh 'docker images'
                sh 'docker ps'
            }
        }
    }
}